<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.project">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">

		<activity
			android:name=".SplashActivity"
			android:theme="@style/SplashActivityTheme">

			<intent-filter>

				<action
					android:name="android.intent.action.MAIN"/>

				<category
					android:name="android.intent.category.LAUNCHER"/>

			</intent-filter>

		</activity>

		<activity
			android:label=""
			android:name=".WelcomeVC"/>

		<activity
			android:label=""
			android:name=".LoginVC"
			android:theme="@style/ActivityLoginVCTheme"/>

		<activity
			android:label=""
			android:name=".SignupVC"
			android:theme="@style/ActivitySignupVCTheme"/>

		<activity
			android:label="Dr. When photos"
			android:name=".ProfilePhotosVC"
			android:theme="@style/ActivityProfilePhotosVCTheme"/>

		<activity
			android:label="Dr. Where settings"
			android:name=".ProfileSettingsVC"
			android:theme="@style/ActivityProfileSettingsVCTheme"/>

		<activity
			android:label="Guess who&apos;s back?"
			android:name=".EventDetailVC"
			android:theme="@style/ActivityEventDetailVCTheme"/>

		<activity
			android:label=""
			android:name=".TabBarHolderActivityOne"/>

	</application>

</manifest>
